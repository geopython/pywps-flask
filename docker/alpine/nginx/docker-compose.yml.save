version: '3.1'

services:
    pywp:
        image: pywps/gunicorn-alpine:3.6
    nginx:
        build: .
        depends_on:
            - gunicorn
        ports:
            - 80:80
        links:
            - gunicorn
